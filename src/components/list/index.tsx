import React from "react";

export default class List extends React.Component {
    render(): React.ReactNode {
        return (
            <div>
                lista
            </div>
        );
    }
}